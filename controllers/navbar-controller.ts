let navbarVisible = false;

export const setNavbarVisible = (visible: boolean) => {
  navbarVisible = visible;
};
export const isNavbarVisible = () => navbarVisible;

export let isNavbarActive: boolean = false;

